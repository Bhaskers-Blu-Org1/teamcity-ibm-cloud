plugins {
  id "java"
}

ext {
  vendorName = "IBM Global Cloud Acceleration Team"
  teamcityVersion = findProperty("teamcityVersion") ?: "2017.2"
}

allprojects {
  version = "0.5.0"
}

repositories {
  mavenCentral()
}

subprojects {
  apply plugin : "java"
}
